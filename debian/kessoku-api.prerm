#!/bin/sh
set -e

service_name=kessoku-api.service

case "$1" in
    remove|deconfigure)
        deb-systemd-invoke stop "$service_name" >/dev/null || true
        deb-systemd-invoke disable "$service_name" >/dev/null || true
        ;;
esac

exit 0
